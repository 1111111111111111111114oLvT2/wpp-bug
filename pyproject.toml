[project]
name = "wpp-bug"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "camoufox[geoip]>=0.5.4",
    "pyyaml>=6.0.3",
]
